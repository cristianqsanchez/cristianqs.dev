---
type Props = {
  title: string
  class?: string
}

const { title } = Astro.props
---

<span
  class={`${Astro.props.class} flex gap-2 items-center py-1 px-2 rounded-lg font-medium`}>
  <slot />
  {title}
</span>
